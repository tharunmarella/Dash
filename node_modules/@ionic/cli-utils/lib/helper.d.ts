import { CLIMeta, IConfig, IPCMessage } from '../definitions';
export interface SendMessageDeps {
    config: IConfig;
    meta: CLIMeta;
}
export declare function sendMessage({config, meta}: SendMessageDeps, msg: IPCMessage): Promise<void>;
