import { IAilmentRegistry, InfoItem, ProjectType } from '../../../definitions';
import { BaseProject } from '../';
import * as doctorLibType from '../../doctor';
export declare class Project extends BaseProject {
    type: ProjectType;
    getInfo(): Promise<InfoItem[]>;
    getAilmentRegistry(deps: doctorLibType.AutomaticallyTreatableAilmentDeps): Promise<IAilmentRegistry>;
    detected(): Promise<boolean>;
    getFrameworkVersion(): Promise<string | undefined>;
    getAppScriptsVersion(): Promise<string | undefined>;
}
